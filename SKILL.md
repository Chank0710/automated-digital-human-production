---
name: heygen-digital-human-video-zh
description: 制作可替换人物、背景、台词、音色、版式和同步知识板的专业数字人视频。用户要求制作 HeyGen 或兼容服务商的数字人、医生口播、专家讲解、AI 主播、透明人物合成视频，或需要一套中文数字人视频工作流时使用。开始生成前必须收集 API、授权和素材，并固定调用已安装的 video-use Skill 完成剪辑与质量检查。
---

# 中文数字人视频制作

## 每次启动先向用户索取以下内容

明确告诉用户需要什么，并标记已经收到的项目。

1. 服务商与 API：使用 HeyGen 或其他数字人服务商；确认账户具备生成权限和足够额度。用户可以直接提供当前任务使用的 API Key，也可以通过环境变量或本地 `.env` 配置。
2. 人物：服务商素材库中的人物名称或 ID，或用户上传的照片、透明图、人物视频；确认用户拥有肖像和数字人使用授权。
3. 背景：用户上传的图片或视频，或允许 Codex 设计背景。
4. 台词：最终文案、目标时长、语言、特殊读音，以及是否允许删减或调整语速。
5. 音色：服务商 Voice ID、已授权的克隆音色、用户上传的录音，或需要的音色特征；涉及声音克隆时确认授权。
6. 交付规格：横竖屏、分辨率、发布平台、整体风格、Logo、品牌色、字幕和同步知识板要求。

用户选择直接提供 API Key 时应当接受，不强制要求用户自行登录 PowerShell 配置。直接提供的密钥仅作为当前任务的临时机密使用，严禁在回复中回显、在日志中打印、写入项目文件、提交到仓库或放入交付物。需要长期复用时，再提供环境变量或被忽略的本地 `.env` 配置方案。

## 固定依赖

每次制作必须使用 `video-use`。开始操作前完整读取它的 `SKILL.md` 和 `helpers/` 中的全部文件。不得修改、替换或复制改写 `video-use`，只把它作为固定的剪辑与质检依赖。

数字人生成服务商可以替换。选择或切换服务商时读取 [服务商适配说明](references/provider-adapter.md)。

## 标准流程

1. 盘点素材并确认人物肖像、声音克隆和素材使用授权。
2. 估算台词时长。发现目标时长不合理时，解释差距并在修改文案或语速前取得确认。
3. 提出人物位置、背景、知识板内容与节奏、音色、画幅和输出规格。用户确认后再生成。
4. 检查 API 身份、额度、人物 ID、音色 ID 和透明背景能力。
5. 服务商支持时，先单独生成语音。检查完整中文、实际时长和首批文字时间戳，再生成付费数字人视频。
6. 用户上传音频后，先用 FFprobe 检测格式、编码、采样率、声道和时长。如果不是 HeyGen 兼容的 WAV，必须先在本地使用 FFmpeg 转换成 PCM 16-bit WAV，并重新检查转换结果。不得要求用户自行转换。
7. 用已经验证的 WAV 或音频 URL 生成数字人。优先请求带原生 Alpha 的透明 WebM。
8. 在后期中合成背景、人物位置、毛玻璃知识板、同步知识点、品牌元素、声音和可选字幕。
9. 检查片头片尾、知识板切换点、代表性画面、人物边缘、音画同步、总时长和音频完整性。
10. 按 `video-use` 规则，把项目产物放入素材目录下的 `edit/`。

服务商文档没有规定其他 WAV 参数时，默认使用：

```powershell
ffmpeg -y -i input_audio -vn -c:a pcm_s16le -ar 48000 -ac 1 output.wav
```

如果 FFprobe 无法读取转换后的 WAV、时长明显异常或没有有效音频流，必须停止上传并修复。

## 中文编码硬规则

- 用户输入、JSON 配置、文件读写和 HTTP 请求全程使用 UTF-8。
- 优先把中文台词写入 UTF-8 JSON 配置文件，再由请求脚本读取。
- PowerShell 通过管道把中文传给 Python 前，必须执行：

```powershell
$OutputEncoding = [System.Text.UTF8Encoding]::new()
```

- `PYTHONIOENCODING` 只影响 Python 输出，不等于修复标准输入编码，不能用它代替 `$OutputEncoding`。
- 生成数字人前先调用 TTS 或低成本验证接口，确认：
  - 时长与台词长度相符；
  - 返回的文字是正常中文，不能是 `?`、乱码或替换字符；
  - 服务商收到了完整台词。
- 长台词只生成一至十秒时，一律判定为异常。先排查编码和请求结构，禁止继续合成为成片。

## 透明通道和视频编码硬规则

- 服务商支持时优先生成透明 WebM。
- 使用 FFprobe 检查 `alpha_mode=1`。即使 `pix_fmt` 显示 `yuv420p`，也不能据此认定没有 Alpha。
- 读取 HeyGen VP9 透明 WebM 时必须强制使用 libvpx VP9 解码器：

```powershell
ffmpeg -c:v libvpx-vp9 -i avatar.webm ...
```

- 缩放和叠加前把人物流转换成 `rgba`。
- 文件带原生 Alpha 时，禁止再次使用黑底 `colorkey` 或 `chromakey`，否则会破坏头发、眼镜、阴影、手指和白色衣物边缘。
- 只有服务商无法输出透明通道时，才使用明确设计的绿幕或蓝幕作为备用方案，并检查溢色、边缘柔化和衣物颜色冲突。

每次生成数字人和最终成片后，运行 [视频检查脚本](scripts/check_video.py)。

## 常用版式建议

根据用户要求调整。医生或专家讲解视频通常采用：

- 人物放在指定一侧，保留安全边距，比例自然，不遮挡重点背景。
- 毛玻璃板由真实背景局部模糊、暖色半透明蒙层、轻阴影、圆角描边和克制的强调色组成。
- 用户要求黑色粗体时，使用清晰的中文粗体。每屏保留一个标题和两至三个短知识点。
- 有字级时间戳时按真实时间同步知识板；没有时按已确认的台词段落和实际音频时长分配。
- 用户需要字幕时，字幕必须位于最终滤镜链的最后。

## 交付前质量门槛

以下项目全部通过后才能交付：

- 完整台词存在，时长合理。
- 声音完整、清晰并与口型同步。
- 人物周围没有黑框、黑边、暗色光晕、绿幕溢色、锯齿或肢体裁切。
- 头发、眼镜、手指、白大褂和运动边缘经得起代表性画面检查。
- 人物没有遮挡知识板或重要背景元素。
- 中文知识点无乱码，字体清楚，切换时间与口播内容一致。
- 分辨率、画幅、时长和输出位置符合确认方案。

检查失败时，说明具体问题，修改后重新渲染并再次检查。禁止把占位视频、截断视频或未经检查的结果称为成片。
